# Exemplo de arquivo de secrets para PRODUCTION
# Copie este arquivo para secrets.yaml e ajuste os valores
# O arquivo secrets.yaml está no .gitignore e não será commitado
# ⚠️ NUNCA commite secrets reais no Git!

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

secretGenerator:
- name: app-secrets
  literals:
  - API_KEY=CHANGE-ME-PRODUCTION
  - DB_PASSWORD=CHANGE-ME-PRODUCTION
  type: Opaque
